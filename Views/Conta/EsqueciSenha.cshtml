@model ByteBank.Forum.ViewModels.ContaEsqueciSenhaViewModel

@{
    ViewBag.Title = "EsqueciSenha";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Recuperar Senha</h2>

<p>Preencha o campo abaixo com seu endereço de e-mail cadastrado no fórum, assim te enviaremos instruções para recuperar sua senha.</p>

@using (Html.BeginForm())
{
    <div class="form-group">
        @Html.ValidationSummary("", new { @class = "text-danger" })

        @Html.EditorForModel()
        <br />
        <button class="btn btn-primary col" type="submit">Enviar E-mail</button>

    </div>
}